//
// This file was generated by stellar_contract_bindings v0.6.0b0
// and stellar_sdk v16.0.0.
//
// This code requires kmp-stellar-sdk (Soneso) v1.9.0 or later.
//
// @generated
//

package com.soneso.stellar.sdk.contract.bindings

import com.soneso.stellar.sdk.KeyPair
import com.soneso.stellar.sdk.Network
import com.soneso.stellar.sdk.contract.AssembledTransaction
import com.soneso.stellar.sdk.contract.ContractClient
import com.soneso.stellar.sdk.scval.Scv
import com.soneso.stellar.sdk.xdr.SCValXdr
import com.soneso.stellar.sdk.xdr.SCValTypeXdr


/**
 * Generated union OptionShapesContractOptUnion
 */
sealed class OptionShapesContractOptUnion {
    abstract fun toSCVal(): SCValXdr

    object Nothing : OptionShapesContractOptUnion() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("Nothing")))
    }

    data class Maybe(
        val value0: UInt?
    ) : OptionShapesContractOptUnion() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("Maybe"),
                if (value0 == null) Scv.toVoid() else Scv.toUint32(value0)
            )
        )
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): OptionShapesContractOptUnion {
            val elements = Scv.fromVec(scVal)
            return when (val tag = Scv.fromSymbol(elements[0])) {
                "Nothing" -> Nothing
                "Maybe" -> Maybe(
                    if (elements[1].discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(elements[1])
                )
                else -> throw IllegalArgumentException("Unknown OptionShapesContractOptUnion tag: $tag")
            }
        }
    }
}

/**
 * Generated struct OptionShapesContractMaybeStruct
 */
data class OptionShapesContractMaybeStruct(
    val flag: UInt,
    val maybe: UInt?
) {
    fun toSCVal(): SCValXdr {
        val map = LinkedHashMap<SCValXdr, SCValXdr>()
        map[Scv.toSymbol("flag")] = Scv.toUint32(flag)
        map[Scv.toSymbol("maybe")] = if (maybe == null) Scv.toVoid() else Scv.toUint32(maybe)
        return Scv.toMap(map)
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): OptionShapesContractMaybeStruct {
            val map = Scv.fromMap(scVal)
            return OptionShapesContractMaybeStruct(
                flag = Scv.fromUint32(map[Scv.toSymbol("flag")]!!),
                maybe = if (map[Scv.toSymbol("maybe")]!!.discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(map[Scv.toSymbol("maybe")]!!)
            )
        }
    }
}

/**
 * Generated contract client for OptionShapesContract.
 *
 * The constructor is internal so same-module tests can inject a preconfigured
 * [ContractClient]; use [forContract] to create instances.
 */
class OptionShapesContract internal constructor(val client: ContractClient) {

    companion object {
        /**
         * Create a client for a deployed contract without loading its spec from the network.
         *
         * The generated methods encode and decode all values themselves, so the on-chain
         * spec download would be a wasted round-trip.
         *
         * @param contractId The contract ID (C... address)
         * @param rpcUrl The Soroban RPC server URL
         * @param network The network the contract is deployed on
         */
        fun forContract(contractId: String, rpcUrl: String, network: Network): OptionShapesContract =
            OptionShapesContract(ContractClient.forContractWithoutSpec(contractId, rpcUrl, network))
    }

    /**
     * Invoke the default contract function.
     * @param value UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return UInt
     */
    suspend fun default(
        `value`: UInt,
        source: String,
        signer: KeyPair?
    ): UInt =
        client.invoke(
            functionName = "default",
            parameters = listOf(Scv.toUint32(`value`)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Build an [AssembledTransaction] for the default contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param value UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildDefaultTx(
        `value`: UInt,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<UInt> =
        client.buildInvoke(
            functionName = "default",
            parameters = listOf(Scv.toUint32(`value`)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Invoke the opt_map contract function.
     * @param map Map<UInt, UInt?>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Map<UInt, UInt?>
     */
    suspend fun optMap(
        map: Map<UInt, UInt?>,
        source: String,
        signer: KeyPair?
    ): Map<UInt, UInt?> =
        client.invoke(
            functionName = "opt_map",
            parameters = listOf(Scv.toMap(LinkedHashMap<SCValXdr, SCValXdr>().apply { map.entries.sortedWith(compareBy { it.key }).forEach { (k0, v0) -> put(Scv.toUint32(k0), if (v0 == null) Scv.toVoid() else Scv.toUint32(v0)) } })),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromMap(result).entries.associate { (k0, v0) -> Scv.fromUint32(k0) to if (v0.discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(v0) } }
        )

    /**
     * Build an [AssembledTransaction] for the opt_map contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param map Map<UInt, UInt?>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildOptMapTx(
        map: Map<UInt, UInt?>,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Map<UInt, UInt?>> =
        client.buildInvoke(
            functionName = "opt_map",
            parameters = listOf(Scv.toMap(LinkedHashMap<SCValXdr, SCValXdr>().apply { map.entries.sortedWith(compareBy { it.key }).forEach { (k0, v0) -> put(Scv.toUint32(k0), if (v0 == null) Scv.toVoid() else Scv.toUint32(v0)) } })),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromMap(result).entries.associate { (k0, v0) -> Scv.fromUint32(k0) to if (v0.discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(v0) } }
        )

    /**
     * Invoke the opt_tuple contract function.
     * @param tuple Pair<UInt?, UInt>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Pair<UInt?, UInt>
     */
    suspend fun optTuple(
        tuple: Pair<UInt?, UInt>,
        source: String,
        signer: KeyPair?
    ): Pair<UInt?, UInt> =
        client.invoke(
            functionName = "opt_tuple",
            parameters = listOf(Scv.toVec(listOf(tuple.first.let { o0 -> if (o0 == null) Scv.toVoid() else Scv.toUint32(o0) }, Scv.toUint32(tuple.second)))),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).let { t0 -> Pair(if (t0[0].discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(t0[0]), Scv.fromUint32(t0[1])) } }
        )

    /**
     * Build an [AssembledTransaction] for the opt_tuple contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param tuple Pair<UInt?, UInt>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildOptTupleTx(
        tuple: Pair<UInt?, UInt>,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Pair<UInt?, UInt>> =
        client.buildInvoke(
            functionName = "opt_tuple",
            parameters = listOf(Scv.toVec(listOf(tuple.first.let { o0 -> if (o0 == null) Scv.toVoid() else Scv.toUint32(o0) }, Scv.toUint32(tuple.second)))),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).let { t0 -> Pair(if (t0[0].discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(t0[0]), Scv.fromUint32(t0[1])) } }
        )

    /**
     * Invoke the opt_union contract function.
     * @param union OptionShapesContractOptUnion
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return OptionShapesContractOptUnion
     */
    suspend fun optUnion(
        union: OptionShapesContractOptUnion,
        source: String,
        signer: KeyPair?
    ): OptionShapesContractOptUnion =
        client.invoke(
            functionName = "opt_union",
            parameters = listOf(union.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> OptionShapesContractOptUnion.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the opt_union contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param union OptionShapesContractOptUnion
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildOptUnionTx(
        union: OptionShapesContractOptUnion,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<OptionShapesContractOptUnion> =
        client.buildInvoke(
            functionName = "opt_union",
            parameters = listOf(union.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> OptionShapesContractOptUnion.fromSCVal(result) }
        )

    /**
     * Invoke the opt_strukt contract function.
     * @param strukt OptionShapesContractMaybeStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return OptionShapesContractMaybeStruct
     */
    suspend fun optStrukt(
        strukt: OptionShapesContractMaybeStruct,
        source: String,
        signer: KeyPair?
    ): OptionShapesContractMaybeStruct =
        client.invoke(
            functionName = "opt_strukt",
            parameters = listOf(strukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> OptionShapesContractMaybeStruct.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the opt_strukt contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param strukt OptionShapesContractMaybeStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildOptStruktTx(
        strukt: OptionShapesContractMaybeStruct,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<OptionShapesContractMaybeStruct> =
        client.buildInvoke(
            functionName = "opt_strukt",
            parameters = listOf(strukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> OptionShapesContractMaybeStruct.fromSCVal(result) }
        )
}
