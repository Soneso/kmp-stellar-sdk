//
// This file was generated by stellar_contract_bindings v0.6.0b0
// and stellar_sdk v16.0.0.
//
// This code requires kmp-stellar-sdk (Soneso) v1.9.0 or later.
//
// The BigInteger-typed 128/256-bit integer parameters and return values use
// com.ionspin.kotlin:bignum, which the SDK re-exports through its public API.
//
// @generated
//

package com.soneso.stellar.sdk.contract.bindings

import com.soneso.stellar.sdk.KeyPair
import com.soneso.stellar.sdk.Network
import com.soneso.stellar.sdk.Address
import com.soneso.stellar.sdk.contract.AssembledTransaction
import com.soneso.stellar.sdk.contract.ContractClient
import com.soneso.stellar.sdk.scval.Scv
import com.soneso.stellar.sdk.xdr.SCValXdr
import com.ionspin.kotlin.bignum.integer.BigInteger


/**
 * Generated contract client for AtomicSwapContract.
 *
 * The constructor is internal so same-module tests can inject a preconfigured
 * [ContractClient]; use [forContract] to create instances.
 */
class AtomicSwapContract internal constructor(val client: ContractClient) {

    companion object {
        /**
         * Create a client for a deployed contract without loading its spec from the network.
         *
         * The generated methods encode and decode all values themselves, so the on-chain
         * spec download would be a wasted round-trip.
         *
         * @param contractId The contract ID (C... address)
         * @param rpcUrl The Soroban RPC server URL
         * @param network The network the contract is deployed on
         */
        fun forContract(contractId: String, rpcUrl: String, network: Network): AtomicSwapContract =
            AtomicSwapContract(ContractClient.forContractWithoutSpec(contractId, rpcUrl, network))
    }

    /**
     * Invoke the swap contract function.
     * @param a Address
     * @param b Address
     * @param tokenA Address
     * @param tokenB Address
     * @param amountA BigInteger
     * @param minBForA BigInteger
     * @param amountB BigInteger
     * @param minAForB BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun swap(
        a: Address,
        b: Address,
        tokenA: Address,
        tokenB: Address,
        amountA: BigInteger,
        minBForA: BigInteger,
        amountB: BigInteger,
        minAForB: BigInteger,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "swap",
            parameters = listOf(a.toSCVal(), b.toSCVal(), tokenA.toSCVal(), tokenB.toSCVal(), Scv.toInt128(amountA), Scv.toInt128(minBForA), Scv.toInt128(amountB), Scv.toInt128(minAForB)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the swap contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param a Address
     * @param b Address
     * @param tokenA Address
     * @param tokenB Address
     * @param amountA BigInteger
     * @param minBForA BigInteger
     * @param amountB BigInteger
     * @param minAForB BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildSwapTx(
        a: Address,
        b: Address,
        tokenA: Address,
        tokenB: Address,
        amountA: BigInteger,
        minBForA: BigInteger,
        amountB: BigInteger,
        minAForB: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "swap",
            parameters = listOf(a.toSCVal(), b.toSCVal(), tokenA.toSCVal(), tokenB.toSCVal(), Scv.toInt128(amountA), Scv.toInt128(minBForA), Scv.toInt128(amountB), Scv.toInt128(minAForB)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
}
