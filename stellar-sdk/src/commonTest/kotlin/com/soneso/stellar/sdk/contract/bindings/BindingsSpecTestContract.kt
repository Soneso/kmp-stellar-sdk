//
// This file was generated by stellar_contract_bindings v0.5.0b0
// and stellar_sdk v13.0.0.
//
// This code requires kmp-stellar-sdk (Soneso) v1.9.0 or later.
//
// The BigInteger-typed 128/256-bit integer parameters and return values use
// com.ionspin.kotlin:bignum, which the SDK re-exports through its public API.
//
// @generated
//

package com.soneso.stellar.sdk.contract.bindings

import com.soneso.stellar.sdk.KeyPair
import com.soneso.stellar.sdk.Network
import com.soneso.stellar.sdk.Address
import com.soneso.stellar.sdk.contract.AssembledTransaction
import com.soneso.stellar.sdk.contract.ContractClient
import com.soneso.stellar.sdk.scval.Scv
import com.soneso.stellar.sdk.xdr.SCValXdr
import com.soneso.stellar.sdk.xdr.SCValTypeXdr
import com.ionspin.kotlin.bignum.integer.BigInteger


/**
 * Generated union BindingsSpecTestContractNone
 */
sealed class BindingsSpecTestContractNone {
    abstract fun toSCVal(): SCValXdr

    object elif : BindingsSpecTestContractNone() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("elif")))
    }

    object nonlocal : BindingsSpecTestContractNone() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("nonlocal")))
    }

    object not : BindingsSpecTestContractNone() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("not")))
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractNone {
            val elements = Scv.fromVec(scVal)
            return when (val tag = Scv.fromSymbol(elements[0])) {
                "elif" -> elif
                "nonlocal" -> nonlocal
                "not" -> not
                else -> throw IllegalArgumentException("Unknown BindingsSpecTestContractNone tag: $tag")
            }
        }
    }
}

/**
 * This is from the rust doc above the struct SimpleStruct
 */
data class BindingsSpecTestContractTrue(
    val def: UInt
) {
    fun toSCVal(): SCValXdr {
        val map = LinkedHashMap<SCValXdr, SCValXdr>()
        map[Scv.toSymbol("def")] = Scv.toUint32(def)
        return Scv.toMap(map)
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractTrue {
            val map = Scv.fromMap(scVal)
            return BindingsSpecTestContractTrue(
                def = Scv.fromUint32(map[Scv.toSymbol("def")]!!)
            )
        }
    }
}

/**
 * Generated error enum BindingsSpecTestContractError
 */
enum class BindingsSpecTestContractError(val value: UInt) {
    NumberMustBeOdd(1u);

    fun toSCVal(): SCValXdr = Scv.toUint32(value)

    companion object {
        fun fromValue(value: UInt): BindingsSpecTestContractError =
            BindingsSpecTestContractError.entries.firstOrNull { it.value == value }
                ?: throw IllegalArgumentException("Unknown BindingsSpecTestContractError value: $value")

        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractError = fromValue(Scv.fromUint32(scVal))
    }
}

/**
 * Generated error enum BindingsSpecTestContractFalse
 */
enum class BindingsSpecTestContractFalse(val value: UInt) {
    elif(1u);

    fun toSCVal(): SCValXdr = Scv.toUint32(value)

    companion object {
        fun fromValue(value: UInt): BindingsSpecTestContractFalse =
            BindingsSpecTestContractFalse.entries.firstOrNull { it.value == value }
                ?: throw IllegalArgumentException("Unknown BindingsSpecTestContractFalse value: $value")

        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractFalse = fromValue(Scv.fromUint32(scVal))
    }
}

/**
 * Generated enum BindingsSpecTestContractimport
 */
enum class BindingsSpecTestContractimport(val value: UInt) {
    not(11u),
    elif(12u);

    fun toSCVal(): SCValXdr = Scv.toUint32(value)

    companion object {
        fun fromValue(value: UInt): BindingsSpecTestContractimport =
            BindingsSpecTestContractimport.entries.firstOrNull { it.value == value }
                ?: throw IllegalArgumentException("Unknown BindingsSpecTestContractimport value: $value")

        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractimport = fromValue(Scv.fromUint32(scVal))
    }
}

/**
 * Generated enum BindingsSpecTestContractRoyalCard
 */
enum class BindingsSpecTestContractRoyalCard(val value: UInt) {
    Jack(11u),
    Queen(12u),
    King(13u);

    fun toSCVal(): SCValXdr = Scv.toUint32(value)

    companion object {
        fun fromValue(value: UInt): BindingsSpecTestContractRoyalCard =
            BindingsSpecTestContractRoyalCard.entries.firstOrNull { it.value == value }
                ?: throw IllegalArgumentException("Unknown BindingsSpecTestContractRoyalCard value: $value")

        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractRoyalCard = fromValue(Scv.fromUint32(scVal))
    }
}

/**
 * Generated union BindingsSpecTestContractSimpleEnum
 */
sealed class BindingsSpecTestContractSimpleEnum {
    abstract fun toSCVal(): SCValXdr

    object First : BindingsSpecTestContractSimpleEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("First")))
    }

    object Second : BindingsSpecTestContractSimpleEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("Second")))
    }

    object Third : BindingsSpecTestContractSimpleEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("Third")))
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractSimpleEnum {
            val elements = Scv.fromVec(scVal)
            return when (val tag = Scv.fromSymbol(elements[0])) {
                "First" -> First
                "Second" -> Second
                "Third" -> Third
                else -> throw IllegalArgumentException("Unknown BindingsSpecTestContractSimpleEnum tag: $tag")
            }
        }
    }
}

/**
 * Generated union BindingsSpecTestContractComplexEnum
 */
sealed class BindingsSpecTestContractComplexEnum {
    abstract fun toSCVal(): SCValXdr

    data class Struct(
        val value0: BindingsSpecTestContractSimpleStruct
    ) : BindingsSpecTestContractComplexEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("Struct"),
                value0.toSCVal()
            )
        )
    }

    data class Tuple(
        val value0: BindingsSpecTestContractTupleStruct
    ) : BindingsSpecTestContractComplexEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("Tuple"),
                value0.toSCVal()
            )
        )
    }

    data class Enum(
        val value0: BindingsSpecTestContractSimpleEnum
    ) : BindingsSpecTestContractComplexEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("Enum"),
                value0.toSCVal()
            )
        )
    }

    data class Asset(
        val value0: Address,
        val value1: BigInteger
    ) : BindingsSpecTestContractComplexEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("Asset"),
                value0.toSCVal(),
                Scv.toInt128(value1)
            )
        )
    }

    object Void : BindingsSpecTestContractComplexEnum() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("Void")))
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractComplexEnum {
            val elements = Scv.fromVec(scVal)
            return when (val tag = Scv.fromSymbol(elements[0])) {
                "Struct" -> Struct(
                    BindingsSpecTestContractSimpleStruct.fromSCVal(elements[1])
                )
                "Tuple" -> Tuple(
                    BindingsSpecTestContractTupleStruct.fromSCVal(elements[1])
                )
                "Enum" -> Enum(
                    BindingsSpecTestContractSimpleEnum.fromSCVal(elements[1])
                )
                "Asset" -> Asset(
                    Address.fromSCVal(elements[1]),
                    Scv.fromInt128(elements[2])
                )
                "Void" -> Void
                else -> throw IllegalArgumentException("Unknown BindingsSpecTestContractComplexEnum tag: $tag")
            }
        }
    }
}

/**
 * Generated tuple struct BindingsSpecTestContractTupleStruct
 */
data class BindingsSpecTestContractTupleStruct(
    val value0: BindingsSpecTestContractSimpleStruct,
    val value1: BindingsSpecTestContractSimpleEnum
) {
    fun toSCVal(): SCValXdr = Scv.toVec(
        listOf(
            value0.toSCVal(),
            value1.toSCVal()
        )
    )

    companion object {
        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractTupleStruct {
            val elements = Scv.fromVec(scVal)
            return BindingsSpecTestContractTupleStruct(
                value0 = BindingsSpecTestContractSimpleStruct.fromSCVal(elements[0]),
                value1 = BindingsSpecTestContractSimpleEnum.fromSCVal(elements[1])
            )
        }
    }
}

/**
 * This is from the rust doc above the struct SimpleStruct
 */
data class BindingsSpecTestContractSimpleStruct(
    val a: UInt,
    val b: Boolean,
    val c: String
) {
    fun toSCVal(): SCValXdr {
        val map = LinkedHashMap<SCValXdr, SCValXdr>()
        map[Scv.toSymbol("a")] = Scv.toUint32(a)
        map[Scv.toSymbol("b")] = Scv.toBoolean(b)
        map[Scv.toSymbol("c")] = Scv.toSymbol(c)
        return Scv.toMap(map)
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): BindingsSpecTestContractSimpleStruct {
            val map = Scv.fromMap(scVal)
            return BindingsSpecTestContractSimpleStruct(
                a = Scv.fromUint32(map[Scv.toSymbol("a")]!!),
                b = Scv.fromBoolean(map[Scv.toSymbol("b")]!!),
                c = Scv.fromSymbol(map[Scv.toSymbol("c")]!!)
            )
        }
    }
}

/**
 * Generated contract client for BindingsSpecTestContract.
 */
class BindingsSpecTestContract internal constructor(val client: ContractClient) {

    companion object {
        /**
         * Create a client for a deployed contract without loading its spec from the network.
         *
         * @param contractId The contract ID (C... address)
         * @param rpcUrl The Soroban RPC server URL
         * @param network The network the contract is deployed on
         */
        fun forContract(contractId: String, rpcUrl: String, network: Network): BindingsSpecTestContract =
            BindingsSpecTestContract(ContractClient.forContractWithoutSpec(contractId, rpcUrl, network))
    }

    /**
     * Invoke the i32 contract function.
     * @param i32 Int
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Int
     */
    suspend fun i32(
        i32: Int,
        source: String,
        signer: KeyPair?
    ): Int =
        client.invoke(
            functionName = "i32",
            parameters = listOf(Scv.toInt32(i32)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt32(result) }
        )

    /**
     * Build an [AssembledTransaction] for the i32 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param i32 Int
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildI32Tx(
        i32: Int,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Int> =
        client.buildInvoke(
            functionName = "i32",
            parameters = listOf(Scv.toInt32(i32)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt32(result) }
        )

    /**
     * Invoke the i64 contract function.
     * @param i64 Long
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Long
     */
    suspend fun i64(
        i64: Long,
        source: String,
        signer: KeyPair?
    ): Long =
        client.invoke(
            functionName = "i64",
            parameters = listOf(Scv.toInt64(i64)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt64(result) }
        )

    /**
     * Build an [AssembledTransaction] for the i64 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param i64 Long
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildI64Tx(
        i64: Long,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Long> =
        client.buildInvoke(
            functionName = "i64",
            parameters = listOf(Scv.toInt64(i64)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt64(result) }
        )

    /**
     * Invoke the map contract function.
     * @param map Map<UInt, Boolean>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Map<UInt, Boolean>
     */
    suspend fun map(
        map: Map<UInt, Boolean>,
        source: String,
        signer: KeyPair?
    ): Map<UInt, Boolean> =
        client.invoke(
            functionName = "map",
            parameters = listOf(Scv.toMap(LinkedHashMap<SCValXdr, SCValXdr>().apply { map.entries.sortedWith(compareBy { it.key }).forEach { (k0, v0) -> put(Scv.toUint32(k0), Scv.toBoolean(v0)) } })),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromMap(result).entries.associate { (k0, v0) -> Scv.fromUint32(k0) to Scv.fromBoolean(v0) } }
        )

    /**
     * Build an [AssembledTransaction] for the map contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param map Map<UInt, Boolean>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildMapTx(
        map: Map<UInt, Boolean>,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Map<UInt, Boolean>> =
        client.buildInvoke(
            functionName = "map",
            parameters = listOf(Scv.toMap(LinkedHashMap<SCValXdr, SCValXdr>().apply { map.entries.sortedWith(compareBy { it.key }).forEach { (k0, v0) -> put(Scv.toUint32(k0), Scv.toBoolean(v0)) } })),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromMap(result).entries.associate { (k0, v0) -> Scv.fromUint32(k0) to Scv.fromBoolean(v0) } }
        )

    /**
     * Negates a boolean value
     * @param boolean Boolean
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Boolean
     */
    suspend fun not(
        boolean: Boolean,
        source: String,
        signer: KeyPair?
    ): Boolean =
        client.invoke(
            functionName = "not",
            parameters = listOf(Scv.toBoolean(boolean)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBoolean(result) }
        )

    /**
     * Build an [AssembledTransaction] for the not contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param boolean Boolean
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildNotTx(
        boolean: Boolean,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Boolean> =
        client.buildInvoke(
            functionName = "not",
            parameters = listOf(Scv.toBoolean(boolean)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBoolean(result) }
        )

    /**
     * Invoke the u32 contract function.
     * @param u32 UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return UInt
     */
    suspend fun u32(
        u32: UInt,
        source: String,
        signer: KeyPair?
    ): UInt =
        client.invoke(
            functionName = "u32",
            parameters = listOf(Scv.toUint32(u32)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Build an [AssembledTransaction] for the u32 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param u32 UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildU32Tx(
        u32: UInt,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<UInt> =
        client.buildInvoke(
            functionName = "u32",
            parameters = listOf(Scv.toUint32(u32)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Invoke the u64 contract function.
     * @param u64 ULong
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return ULong
     */
    suspend fun u64(
        u64: ULong,
        source: String,
        signer: KeyPair?
    ): ULong =
        client.invoke(
            functionName = "u64",
            parameters = listOf(Scv.toUint64(u64)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint64(result) }
        )

    /**
     * Build an [AssembledTransaction] for the u64 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param u64 ULong
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildU64Tx(
        u64: ULong,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<ULong> =
        client.buildInvoke(
            functionName = "u64",
            parameters = listOf(Scv.toUint64(u64)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint64(result) }
        )

    /**
     * Invoke the val contract function.
     * @param a UInt
     * @param b SCValXdr
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return SCValXdr
     */
    suspend fun `val`(
        a: UInt,
        b: SCValXdr,
        source: String,
        signer: KeyPair?
    ): SCValXdr =
        client.invoke(
            functionName = "val",
            parameters = listOf(Scv.toUint32(a), b),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> result }
        )

    /**
     * Build an [AssembledTransaction] for the val contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param a UInt
     * @param b SCValXdr
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildValTx(
        a: UInt,
        b: SCValXdr,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<SCValXdr> =
        client.buildInvoke(
            functionName = "val",
            parameters = listOf(Scv.toUint32(a), b),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> result }
        )

    /**
     * Invoke the vec contract function.
     * @param vec List<UInt>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return List<UInt>
     */
    suspend fun vec(
        vec: List<UInt>,
        source: String,
        signer: KeyPair?
    ): List<UInt> =
        client.invoke(
            functionName = "vec",
            parameters = listOf(Scv.toVec(vec.map { v0 -> Scv.toUint32(v0) })),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).map { e0 -> Scv.fromUint32(e0) } }
        )

    /**
     * Build an [AssembledTransaction] for the vec contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param vec List<UInt>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildVecTx(
        vec: List<UInt>,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<List<UInt>> =
        client.buildInvoke(
            functionName = "vec",
            parameters = listOf(Scv.toVec(vec.map { v0 -> Scv.toUint32(v0) })),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).map { e0 -> Scv.fromUint32(e0) } }
        )

    /**
     * Invoke the card contract function.
     * @param card BindingsSpecTestContractRoyalCard
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BindingsSpecTestContractRoyalCard
     */
    suspend fun card(
        card: BindingsSpecTestContractRoyalCard,
        source: String,
        signer: KeyPair?
    ): BindingsSpecTestContractRoyalCard =
        client.invoke(
            functionName = "card",
            parameters = listOf(card.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractRoyalCard.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the card contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param card BindingsSpecTestContractRoyalCard
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildCardTx(
        card: BindingsSpecTestContractRoyalCard,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BindingsSpecTestContractRoyalCard> =
        client.buildInvoke(
            functionName = "card",
            parameters = listOf(card.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractRoyalCard.fromSCVal(result) }
        )

    /**
     * Invoke the from contract function.
     * @param `finally` String
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return String
     */
    suspend fun from(
        `finally`: String,
        source: String,
        signer: KeyPair?
    ): String =
        client.invoke(
            functionName = "from",
            parameters = listOf(Scv.toSymbol(`finally`)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromSymbol(result) }
        )

    /**
     * Build an [AssembledTransaction] for the from contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param `finally` String
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildFromTx(
        `finally`: String,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<String> =
        client.buildInvoke(
            functionName = "from",
            parameters = listOf(Scv.toSymbol(`finally`)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromSymbol(result) }
        )

    /**
     * Invoke the i128 contract function.
     * @param i128 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BigInteger
     */
    suspend fun i128(
        i128: BigInteger,
        source: String,
        signer: KeyPair?
    ): BigInteger =
        client.invoke(
            functionName = "i128",
            parameters = listOf(Scv.toInt128(i128)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt128(result) }
        )

    /**
     * Build an [AssembledTransaction] for the i128 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param i128 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildI128Tx(
        i128: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BigInteger> =
        client.buildInvoke(
            functionName = "i128",
            parameters = listOf(Scv.toInt128(i128)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt128(result) }
        )

    /**
     * Invoke the i256 contract function.
     * @param i256 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BigInteger
     */
    suspend fun i256(
        i256: BigInteger,
        source: String,
        signer: KeyPair?
    ): BigInteger =
        client.invoke(
            functionName = "i256",
            parameters = listOf(Scv.toInt256(i256)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt256(result) }
        )

    /**
     * Build an [AssembledTransaction] for the i256 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param i256 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildI256Tx(
        i256: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BigInteger> =
        client.buildInvoke(
            functionName = "i256",
            parameters = listOf(Scv.toInt256(i256)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt256(result) }
        )

    /**
     * Invoke the u128 contract function.
     * @param u128 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BigInteger
     */
    suspend fun u128(
        u128: BigInteger,
        source: String,
        signer: KeyPair?
    ): BigInteger =
        client.invoke(
            functionName = "u128",
            parameters = listOf(Scv.toUint128(u128)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint128(result) }
        )

    /**
     * Build an [AssembledTransaction] for the u128 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param u128 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildU128Tx(
        u128: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BigInteger> =
        client.buildInvoke(
            functionName = "u128",
            parameters = listOf(Scv.toUint128(u128)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint128(result) }
        )

    /**
     * Invoke the u256 contract function.
     * @param u256 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BigInteger
     */
    suspend fun u256(
        u256: BigInteger,
        source: String,
        signer: KeyPair?
    ): BigInteger =
        client.invoke(
            functionName = "u256",
            parameters = listOf(Scv.toUint256(u256)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint256(result) }
        )

    /**
     * Build an [AssembledTransaction] for the u256 contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param u256 BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildU256Tx(
        u256: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BigInteger> =
        client.buildInvoke(
            functionName = "u256",
            parameters = listOf(Scv.toUint256(u256)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint256(result) }
        )

    /**
     * Invoke the void contract function.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun void(
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "void",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the void contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildVoidTx(
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "void",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the hello contract function.
     * @param hello String
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return String
     */
    suspend fun hello(
        hello: String,
        source: String,
        signer: KeyPair?
    ): String =
        client.invoke(
            functionName = "hello",
            parameters = listOf(Scv.toSymbol(hello)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromSymbol(result) }
        )

    /**
     * Build an [AssembledTransaction] for the hello contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param hello String
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildHelloTx(
        hello: String,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<String> =
        client.buildInvoke(
            functionName = "hello",
            parameters = listOf(Scv.toSymbol(hello)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromSymbol(result) }
        )

    /**
     * Invoke the tuple contract function.
     * @param tuple Pair<String, UInt>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Pair<String, UInt>
     */
    suspend fun tuple(
        tuple: Pair<String, UInt>,
        source: String,
        signer: KeyPair?
    ): Pair<String, UInt> =
        client.invoke(
            functionName = "tuple",
            parameters = listOf(Scv.toVec(listOf(Scv.toSymbol(tuple.first), Scv.toUint32(tuple.second)))),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).let { t0 -> Pair(Scv.fromSymbol(t0[0]), Scv.fromUint32(t0[1])) } }
        )

    /**
     * Build an [AssembledTransaction] for the tuple contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param tuple Pair<String, UInt>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildTupleTx(
        tuple: Pair<String, UInt>,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Pair<String, UInt>> =
        client.buildInvoke(
            functionName = "tuple",
            parameters = listOf(Scv.toVec(listOf(Scv.toSymbol(tuple.first), Scv.toUint32(tuple.second)))),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).let { t0 -> Pair(Scv.fromSymbol(t0[0]), Scv.fromUint32(t0[1])) } }
        )

    /**
     * Invoke the bytes_ contract function.
     * @param bytes ByteArray
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return ByteArray
     */
    suspend fun bytes(
        bytes: ByteArray,
        source: String,
        signer: KeyPair?
    ): ByteArray =
        client.invoke(
            functionName = "bytes_",
            parameters = listOf(Scv.toBytes(bytes)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBytes(result) }
        )

    /**
     * Build an [AssembledTransaction] for the bytes_ contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param bytes ByteArray
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildBytesTx(
        bytes: ByteArray,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<ByteArray> =
        client.buildInvoke(
            functionName = "bytes_",
            parameters = listOf(Scv.toBytes(bytes)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBytes(result) }
        )

    /**
     * Example of an optional argument
     * @param option UInt?
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return UInt?
     */
    suspend fun option(
        option: UInt?,
        source: String,
        signer: KeyPair?
    ): UInt? =
        client.invoke(
            functionName = "option",
            parameters = listOf(if (option == null) Scv.toVoid() else Scv.toUint32(option)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> if (result.discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(result) }
        )

    /**
     * Build an [AssembledTransaction] for the option contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param option UInt?
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildOptionTx(
        option: UInt?,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<UInt?> =
        client.buildInvoke(
            functionName = "option",
            parameters = listOf(if (option == null) Scv.toVoid() else Scv.toUint32(option)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> if (result.discriminant == SCValTypeXdr.SCV_VOID) null else Scv.fromUint32(result) }
        )

    /**
     * Invoke the simple contract function.
     * @param simple BindingsSpecTestContractSimpleEnum
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BindingsSpecTestContractSimpleEnum
     */
    suspend fun simple(
        simple: BindingsSpecTestContractSimpleEnum,
        source: String,
        signer: KeyPair?
    ): BindingsSpecTestContractSimpleEnum =
        client.invoke(
            functionName = "simple",
            parameters = listOf(simple.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractSimpleEnum.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the simple contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param simple BindingsSpecTestContractSimpleEnum
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildSimpleTx(
        simple: BindingsSpecTestContractSimpleEnum,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BindingsSpecTestContractSimpleEnum> =
        client.buildInvoke(
            functionName = "simple",
            parameters = listOf(simple.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractSimpleEnum.fromSCVal(result) }
        )

    /**
     * Invoke the string contract function.
     * @param string String
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return String
     */
    suspend fun string(
        string: String,
        source: String,
        signer: KeyPair?
    ): String =
        client.invoke(
            functionName = "string",
            parameters = listOf(Scv.toString(string)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromString(result) }
        )

    /**
     * Build an [AssembledTransaction] for the string contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param string String
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildStringTx(
        string: String,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<String> =
        client.buildInvoke(
            functionName = "string",
            parameters = listOf(Scv.toString(string)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromString(result) }
        )

    /**
     * Invoke the strukt contract function.
     * @param strukt BindingsSpecTestContractSimpleStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BindingsSpecTestContractSimpleStruct
     */
    suspend fun strukt(
        strukt: BindingsSpecTestContractSimpleStruct,
        source: String,
        signer: KeyPair?
    ): BindingsSpecTestContractSimpleStruct =
        client.invoke(
            functionName = "strukt",
            parameters = listOf(strukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractSimpleStruct.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the strukt contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param strukt BindingsSpecTestContractSimpleStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildStruktTx(
        strukt: BindingsSpecTestContractSimpleStruct,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BindingsSpecTestContractSimpleStruct> =
        client.buildInvoke(
            functionName = "strukt",
            parameters = listOf(strukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractSimpleStruct.fromSCVal(result) }
        )

    /**
     * Invoke the address contract function.
     * @param address Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Address
     */
    suspend fun address(
        address: Address,
        source: String,
        signer: KeyPair?
    ): Address =
        client.invoke(
            functionName = "address",
            parameters = listOf(address.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Address.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the address contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param address Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildAddressTx(
        address: Address,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Address> =
        client.buildInvoke(
            functionName = "address",
            parameters = listOf(address.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Address.fromSCVal(result) }
        )

    /**
     * Invoke the boolean contract function.
     * @param boolean Boolean
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Boolean
     */
    suspend fun boolean(
        boolean: Boolean,
        source: String,
        signer: KeyPair?
    ): Boolean =
        client.invoke(
            functionName = "boolean",
            parameters = listOf(Scv.toBoolean(boolean)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBoolean(result) }
        )

    /**
     * Build an [AssembledTransaction] for the boolean contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param boolean Boolean
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildBooleanTx(
        boolean: Boolean,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Boolean> =
        client.buildInvoke(
            functionName = "boolean",
            parameters = listOf(Scv.toBoolean(boolean)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBoolean(result) }
        )

    /**
     * Invoke the bytes_n contract function.
     * @param bytesN ByteArray
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return ByteArray
     */
    suspend fun bytesN(
        bytesN: ByteArray,
        source: String,
        signer: KeyPair?
    ): ByteArray =
        client.invoke(
            functionName = "bytes_n",
            parameters = listOf(Scv.toBytes(bytesN)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBytes(result) }
        )

    /**
     * Build an [AssembledTransaction] for the bytes_n contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param bytesN ByteArray
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildBytesNTx(
        bytesN: ByteArray,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<ByteArray> =
        client.buildInvoke(
            functionName = "bytes_n",
            parameters = listOf(Scv.toBytes(bytesN)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromBytes(result) }
        )

    /**
     * Invoke the complex contract function.
     * @param complex BindingsSpecTestContractComplexEnum
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BindingsSpecTestContractComplexEnum
     */
    suspend fun complex(
        complex: BindingsSpecTestContractComplexEnum,
        source: String,
        signer: KeyPair?
    ): BindingsSpecTestContractComplexEnum =
        client.invoke(
            functionName = "complex",
            parameters = listOf(complex.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractComplexEnum.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the complex contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param complex BindingsSpecTestContractComplexEnum
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildComplexTx(
        complex: BindingsSpecTestContractComplexEnum,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BindingsSpecTestContractComplexEnum> =
        client.buildInvoke(
            functionName = "complex",
            parameters = listOf(complex.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractComplexEnum.fromSCVal(result) }
        )

    /**
     * Invoke the duration contract function.
     * @param duration ULong
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return ULong
     */
    suspend fun duration(
        duration: ULong,
        source: String,
        signer: KeyPair?
    ): ULong =
        client.invoke(
            functionName = "duration",
            parameters = listOf(Scv.toDuration(duration)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromDuration(result) }
        )

    /**
     * Build an [AssembledTransaction] for the duration contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param duration ULong
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildDurationTx(
        duration: ULong,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<ULong> =
        client.buildInvoke(
            functionName = "duration",
            parameters = listOf(Scv.toDuration(duration)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromDuration(result) }
        )

    /**
     * Invoke the timepoint contract function.
     * @param timepoint ULong
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return ULong
     */
    suspend fun timepoint(
        timepoint: ULong,
        source: String,
        signer: KeyPair?
    ): ULong =
        client.invoke(
            functionName = "timepoint",
            parameters = listOf(Scv.toTimePoint(timepoint)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromTimePoint(result) }
        )

    /**
     * Build an [AssembledTransaction] for the timepoint contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param timepoint ULong
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildTimepointTx(
        timepoint: ULong,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<ULong> =
        client.buildInvoke(
            functionName = "timepoint",
            parameters = listOf(Scv.toTimePoint(timepoint)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromTimePoint(result) }
        )

    /**
     * Invoke the multi_args contract function.
     * @param a UInt
     * @param b Boolean
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return UInt
     */
    suspend fun multiArgs(
        a: UInt,
        b: Boolean,
        source: String,
        signer: KeyPair?
    ): UInt =
        client.invoke(
            functionName = "multi_args",
            parameters = listOf(Scv.toUint32(a), Scv.toBoolean(b)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Build an [AssembledTransaction] for the multi_args contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param a UInt
     * @param b Boolean
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildMultiArgsTx(
        a: UInt,
        b: Boolean,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<UInt> =
        client.buildInvoke(
            functionName = "multi_args",
            parameters = listOf(Scv.toUint32(a), Scv.toBoolean(b)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Example contract method which takes a struct
     * @param strukt BindingsSpecTestContractSimpleStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return List<String>
     */
    suspend fun struktHel(
        strukt: BindingsSpecTestContractSimpleStruct,
        source: String,
        signer: KeyPair?
    ): List<String> =
        client.invoke(
            functionName = "strukt_hel",
            parameters = listOf(strukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).map { e0 -> Scv.fromSymbol(e0) } }
        )

    /**
     * Build an [AssembledTransaction] for the strukt_hel contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param strukt BindingsSpecTestContractSimpleStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildStruktHelTx(
        strukt: BindingsSpecTestContractSimpleStruct,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<List<String>> =
        client.buildInvoke(
            functionName = "strukt_hel",
            parameters = listOf(strukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).map { e0 -> Scv.fromSymbol(e0) } }
        )

    /**
     * Invoke the empty_tuple contract function.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun emptyTuple(
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "empty_tuple",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the empty_tuple contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildEmptyTupleTx(
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "empty_tuple",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the tuple_strukt contract function.
     * @param tupleStrukt BindingsSpecTestContractTupleStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BindingsSpecTestContractTupleStruct
     */
    suspend fun tupleStrukt(
        tupleStrukt: BindingsSpecTestContractTupleStruct,
        source: String,
        signer: KeyPair?
    ): BindingsSpecTestContractTupleStruct =
        client.invoke(
            functionName = "tuple_strukt",
            parameters = listOf(tupleStrukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractTupleStruct.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the tuple_strukt contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param tupleStrukt BindingsSpecTestContractTupleStruct
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildTupleStruktTx(
        tupleStrukt: BindingsSpecTestContractTupleStruct,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BindingsSpecTestContractTupleStruct> =
        client.buildInvoke(
            functionName = "tuple_strukt",
            parameters = listOf(tupleStrukt.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> BindingsSpecTestContractTupleStruct.fromSCVal(result) }
        )

    /**
     * Invoke the muxed_address contract function.
     * @param address Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Address
     */
    suspend fun muxedAddress(
        address: Address,
        source: String,
        signer: KeyPair?
    ): Address =
        client.invoke(
            functionName = "muxed_address",
            parameters = listOf(address.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Address.fromSCVal(result) }
        )

    /**
     * Build an [AssembledTransaction] for the muxed_address contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param address Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildMuxedAddressTx(
        address: Address,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Address> =
        client.buildInvoke(
            functionName = "muxed_address",
            parameters = listOf(address.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Address.fromSCVal(result) }
        )

    /**
     * Invoke the u32_fail_on_even contract function.
     * @param u32 UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return UInt
     */
    suspend fun u32FailOnEven(
        u32: UInt,
        source: String,
        signer: KeyPair?
    ): UInt =
        client.invoke(
            functionName = "u32_fail_on_even",
            parameters = listOf(Scv.toUint32(u32)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Build an [AssembledTransaction] for the u32_fail_on_even contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param u32 UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildU32FailOnEvenTx(
        u32: UInt,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<UInt> =
        client.buildInvoke(
            functionName = "u32_fail_on_even",
            parameters = listOf(Scv.toUint32(u32)),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Invoke the tuple_strukt_nested contract function.
     * @param tupleStrukt Pair<BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return Pair<BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum>
     */
    suspend fun tupleStruktNested(
        tupleStrukt: Pair<BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum>,
        source: String,
        signer: KeyPair?
    ): Pair<BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum> =
        client.invoke(
            functionName = "tuple_strukt_nested",
            parameters = listOf(Scv.toVec(listOf(tupleStrukt.first.toSCVal(), tupleStrukt.second.toSCVal()))),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).let { t0 -> Pair(BindingsSpecTestContractSimpleStruct.fromSCVal(t0[0]), BindingsSpecTestContractSimpleEnum.fromSCVal(t0[1])) } }
        )

    /**
     * Build an [AssembledTransaction] for the tuple_strukt_nested contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param tupleStrukt Pair<BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum>
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildTupleStruktNestedTx(
        tupleStrukt: Pair<BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum>,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Pair<BindingsSpecTestContractSimpleStruct, BindingsSpecTestContractSimpleEnum>> =
        client.buildInvoke(
            functionName = "tuple_strukt_nested",
            parameters = listOf(Scv.toVec(listOf(tupleStrukt.first.toSCVal(), tupleStrukt.second.toSCVal()))),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromVec(result).let { t0 -> Pair(BindingsSpecTestContractSimpleStruct.fromSCVal(t0[0]), BindingsSpecTestContractSimpleEnum.fromSCVal(t0[1])) } }
        )
}
