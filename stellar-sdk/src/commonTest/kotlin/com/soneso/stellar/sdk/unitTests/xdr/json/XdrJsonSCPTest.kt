// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package com.soneso.stellar.sdk.unitTests.xdr.json

import com.soneso.stellar.sdk.xdr.*
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertFailsWith
import kotlinx.serialization.json.JsonNull
import kotlinx.serialization.json.JsonPrimitive
import kotlinx.serialization.json.buildJsonObject

internal fun sampleValueXdr(seed: Int, depth: Int): ValueXdr =
  ValueXdr(sampleBytes(seed, 4))

internal fun sampleSCPBallotXdr(seed: Int, depth: Int): SCPBallotXdr = SCPBallotXdr(
  sampleUint32Xdr(seed, depth + 1),
  sampleValueXdr(seed + 3, depth + 1)
)

internal fun sampleSCPStatementTypeXdr(seed: Int): SCPStatementTypeXdr = SCPStatementTypeXdr.entries[seed.mod(SCPStatementTypeXdr.entries.size)]

internal fun sampleSCPNominationXdr(seed: Int, depth: Int): SCPNominationXdr = SCPNominationXdr(
  sampleHashXdr(seed, depth + 1),
  if (depth < SAMPLE_DEPTH) List(1) { sampleValueXdr(seed + 3 + it, depth + 1) } else emptyList(),
  if (depth < SAMPLE_DEPTH) List(1) { sampleValueXdr(seed + 6 + it, depth + 1) } else emptyList()
)

internal fun sampleSCPStatementPrepareXdr(seed: Int, depth: Int): SCPStatementPrepareXdr = SCPStatementPrepareXdr(
  sampleHashXdr(seed, depth + 1),
  sampleSCPBallotXdr(seed + 3, depth + 1),
  if (depth < SAMPLE_DEPTH) sampleSCPBallotXdr(seed + 6, depth + 1) else null,
  if (depth < SAMPLE_DEPTH) sampleSCPBallotXdr(seed + 9, depth + 1) else null,
  sampleUint32Xdr(seed + 12, depth + 1),
  sampleUint32Xdr(seed + 15, depth + 1)
)

internal fun sampleSCPStatementConfirmXdr(seed: Int, depth: Int): SCPStatementConfirmXdr = SCPStatementConfirmXdr(
  sampleSCPBallotXdr(seed, depth + 1),
  sampleUint32Xdr(seed + 3, depth + 1),
  sampleUint32Xdr(seed + 6, depth + 1),
  sampleUint32Xdr(seed + 9, depth + 1),
  sampleHashXdr(seed + 12, depth + 1)
)

internal fun sampleSCPStatementExternalizeXdr(seed: Int, depth: Int): SCPStatementExternalizeXdr = SCPStatementExternalizeXdr(
  sampleSCPBallotXdr(seed, depth + 1),
  sampleUint32Xdr(seed + 3, depth + 1),
  sampleHashXdr(seed + 6, depth + 1)
)

internal fun sampleSCPStatementPledgesXdrPrepareArm(seed: Int, depth: Int): SCPStatementPledgesXdr =
  SCPStatementPledgesXdr.Prepare(sampleSCPStatementPrepareXdr(seed, depth + 1))

internal fun sampleSCPStatementPledgesXdrConfirmArm(seed: Int, depth: Int): SCPStatementPledgesXdr =
  SCPStatementPledgesXdr.Confirm(sampleSCPStatementConfirmXdr(seed, depth + 1))

internal fun sampleSCPStatementPledgesXdrExternalizeArm(seed: Int, depth: Int): SCPStatementPledgesXdr =
  SCPStatementPledgesXdr.Externalize(sampleSCPStatementExternalizeXdr(seed, depth + 1))

internal fun sampleSCPStatementPledgesXdrNominateArm(seed: Int, depth: Int): SCPStatementPledgesXdr =
  SCPStatementPledgesXdr.Nominate(sampleSCPNominationXdr(seed, depth + 1))

internal fun sampleSCPStatementPledgesXdr(seed: Int, depth: Int): SCPStatementPledgesXdr =
  when (seed.mod(4)) {
    0 -> sampleSCPStatementPledgesXdrPrepareArm(seed, depth)
    1 -> sampleSCPStatementPledgesXdrConfirmArm(seed + 3, depth)
    2 -> sampleSCPStatementPledgesXdrExternalizeArm(seed + 6, depth)
    else -> sampleSCPStatementPledgesXdrNominateArm(seed + 9, depth)
  }

internal fun sampleSCPStatementXdr(seed: Int, depth: Int): SCPStatementXdr = SCPStatementXdr(
  sampleNodeIDXdr(seed, depth + 1),
  sampleUint64Xdr(seed + 3, depth + 1),
  sampleSCPStatementPledgesXdr(seed + 6, depth + 1)
)

internal fun sampleSCPEnvelopeXdr(seed: Int, depth: Int): SCPEnvelopeXdr = SCPEnvelopeXdr(
  sampleSCPStatementXdr(seed, depth + 1),
  sampleSignatureXdr(seed + 3, depth + 1)
)

internal fun sampleSCPQuorumSetXdr(seed: Int, depth: Int): SCPQuorumSetXdr = SCPQuorumSetXdr(
  sampleUint32Xdr(seed, depth + 1),
  if (depth < SAMPLE_DEPTH) List(1) { sampleNodeIDXdr(seed + 3 + it, depth + 1) } else emptyList(),
  if (depth < SAMPLE_DEPTH) List(1) { sampleSCPQuorumSetXdr(seed + 6 + it, depth + 1) } else emptyList()
)

/**
 * XDR-JSON (SEP-0051) rendering, decoding and rejection behaviour of the types
 * generated from Stellar-SCP.x.
 */
class XdrJsonSCPTest {

  @Test
  fun valueXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripValueXdr(sampleValueXdr(7, 0))
    assertRoundTripValueXdr(sampleValueXdr(7, SAMPLE_DEPTH))
  }

  private fun assertRoundTripValueXdr(value: ValueXdr) {
    val tree = value.toXdrJsonElement()
    val text = ValueXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, ValueXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, ValueXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    ValueXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, ValueXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun valueXdrRejectsInputOfTheWrongShape() {
    assertFailsWith<IllegalArgumentException> { ValueXdr.fromXdrJsonElement(JsonNull) }
  }


  @Test
  fun sCPBallotXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPBallotXdr(sampleSCPBallotXdr(108, 0))
    assertRoundTripSCPBallotXdr(sampleSCPBallotXdr(108, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPBallotXdr(value: SCPBallotXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPBallotXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPBallotXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPBallotXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPBallotXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPBallotXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPBallotXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPBallotXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPBallotXdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCPStatementTypeXdrRendersScpStPrepareAsPrepare() {
    val member = SCPStatementTypeXdr.SCP_ST_PREPARE
    assertEquals(0, member.value)
    assertEquals(JsonPrimitive("prepare"), member.toXdrJsonElement())
    assertEquals("\"prepare\"", member.toXdrJson())
    assertEquals(member, SCPStatementTypeXdr.fromXdrJson("\"prepare\""))
    assertEquals(member, SCPStatementTypeXdr.fromXdrJsonElement(JsonPrimitive("prepare")))
  }

  @Test
  fun sCPStatementTypeXdrRendersScpStConfirmAsConfirm() {
    val member = SCPStatementTypeXdr.SCP_ST_CONFIRM
    assertEquals(1, member.value)
    assertEquals(JsonPrimitive("confirm"), member.toXdrJsonElement())
    assertEquals("\"confirm\"", member.toXdrJson())
    assertEquals(member, SCPStatementTypeXdr.fromXdrJson("\"confirm\""))
    assertEquals(member, SCPStatementTypeXdr.fromXdrJsonElement(JsonPrimitive("confirm")))
  }

  @Test
  fun sCPStatementTypeXdrRendersScpStExternalizeAsExternalize() {
    val member = SCPStatementTypeXdr.SCP_ST_EXTERNALIZE
    assertEquals(2, member.value)
    assertEquals(JsonPrimitive("externalize"), member.toXdrJsonElement())
    assertEquals("\"externalize\"", member.toXdrJson())
    assertEquals(member, SCPStatementTypeXdr.fromXdrJson("\"externalize\""))
    assertEquals(member, SCPStatementTypeXdr.fromXdrJsonElement(JsonPrimitive("externalize")))
  }

  @Test
  fun sCPStatementTypeXdrRendersScpStNominateAsNominate() {
    val member = SCPStatementTypeXdr.SCP_ST_NOMINATE
    assertEquals(3, member.value)
    assertEquals(JsonPrimitive("nominate"), member.toXdrJsonElement())
    assertEquals("\"nominate\"", member.toXdrJson())
    assertEquals(member, SCPStatementTypeXdr.fromXdrJson("\"nominate\""))
    assertEquals(member, SCPStatementTypeXdr.fromXdrJsonElement(JsonPrimitive("nominate")))
  }

  @Test
  fun sCPStatementTypeXdrRejectsAMemberItDoesNotDeclare() {
    assertFailsWith<IllegalArgumentException> { SCPStatementTypeXdr.fromXdrJsonElement(JsonPrimitive("not_a_name_this_type_declares")) }
    assertFailsWith<IllegalArgumentException> { SCPStatementTypeXdr.fromXdrJsonElement(JsonNull) }
  }


  @Test
  fun sCPNominationXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPNominationXdr(sampleSCPNominationXdr(310, 0))
    assertRoundTripSCPNominationXdr(sampleSCPNominationXdr(310, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPNominationXdr(value: SCPNominationXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPNominationXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPNominationXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPNominationXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPNominationXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPNominationXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPNominationXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPNominationXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPNominationXdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCPStatementPrepareXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPStatementPrepareXdr(sampleSCPStatementPrepareXdr(411, 0))
    assertRoundTripSCPStatementPrepareXdr(sampleSCPStatementPrepareXdr(411, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPStatementPrepareXdr(value: SCPStatementPrepareXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPStatementPrepareXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPStatementPrepareXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPStatementPrepareXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPStatementPrepareXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPStatementPrepareXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPStatementPrepareXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPStatementPrepareXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPStatementPrepareXdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCPStatementConfirmXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPStatementConfirmXdr(sampleSCPStatementConfirmXdr(512, 0))
    assertRoundTripSCPStatementConfirmXdr(sampleSCPStatementConfirmXdr(512, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPStatementConfirmXdr(value: SCPStatementConfirmXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPStatementConfirmXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPStatementConfirmXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPStatementConfirmXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPStatementConfirmXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPStatementConfirmXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPStatementConfirmXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPStatementConfirmXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPStatementConfirmXdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCPStatementExternalizeXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPStatementExternalizeXdr(sampleSCPStatementExternalizeXdr(613, 0))
    assertRoundTripSCPStatementExternalizeXdr(sampleSCPStatementExternalizeXdr(613, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPStatementExternalizeXdr(value: SCPStatementExternalizeXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPStatementExternalizeXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPStatementExternalizeXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPStatementExternalizeXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPStatementExternalizeXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPStatementExternalizeXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPStatementExternalizeXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPStatementExternalizeXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPStatementExternalizeXdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCPStatementPledgesXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPStatementPledgesXdr(sampleSCPStatementPledgesXdr(714, 0))
    assertRoundTripSCPStatementPledgesXdr(sampleSCPStatementPledgesXdr(714, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPStatementPledgesXdr(value: SCPStatementPledgesXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPStatementPledgesXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPStatementPledgesXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPStatementPledgesXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPStatementPledgesXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPStatementPledgesXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPStatementPledgesXdrRendersPrepareArmAsPrepare() {
    val value = sampleSCPStatementPledgesXdrPrepareArm(714, 0)
    val tree = value.toXdrJsonElement()
    assertEquals(listOf("prepare"), jsonKeys(tree))
    val decoded = SCPStatementPledgesXdr.fromXdrJsonElement(tree)
    assertEquals(value.discriminant, decoded.discriminant)
    assertEquals(tree, decoded.toXdrJsonElement())
  }

  @Test
  fun sCPStatementPledgesXdrRendersConfirmArmAsConfirm() {
    val value = sampleSCPStatementPledgesXdrConfirmArm(719, 0)
    val tree = value.toXdrJsonElement()
    assertEquals(listOf("confirm"), jsonKeys(tree))
    val decoded = SCPStatementPledgesXdr.fromXdrJsonElement(tree)
    assertEquals(value.discriminant, decoded.discriminant)
    assertEquals(tree, decoded.toXdrJsonElement())
  }

  @Test
  fun sCPStatementPledgesXdrRendersExternalizeArmAsExternalize() {
    val value = sampleSCPStatementPledgesXdrExternalizeArm(724, 0)
    val tree = value.toXdrJsonElement()
    assertEquals(listOf("externalize"), jsonKeys(tree))
    val decoded = SCPStatementPledgesXdr.fromXdrJsonElement(tree)
    assertEquals(value.discriminant, decoded.discriminant)
    assertEquals(tree, decoded.toXdrJsonElement())
  }

  @Test
  fun sCPStatementPledgesXdrRendersNominateArmAsNominate() {
    val value = sampleSCPStatementPledgesXdrNominateArm(729, 0)
    val tree = value.toXdrJsonElement()
    assertEquals(listOf("nominate"), jsonKeys(tree))
    val decoded = SCPStatementPledgesXdr.fromXdrJsonElement(tree)
    assertEquals(value.discriminant, decoded.discriminant)
    assertEquals(tree, decoded.toXdrJsonElement())
  }

  @Test
  fun sCPStatementPledgesXdrRejectsAnArmItDoesNotDeclare() {
    assertFailsWith<IllegalArgumentException> {
      SCPStatementPledgesXdr.fromXdrJsonElement(buildJsonObject { put("not_a_name_this_type_declares", JsonNull) })
    }
    assertFailsWith<IllegalArgumentException> {
      SCPStatementPledgesXdr.fromXdrJsonElement(
        buildJsonObject {
          put("not_a_name_this_type_declares", JsonNull)
          put("not_a_name_this_type_declares_too", JsonNull)
        }
      )
    }
    assertFailsWith<IllegalArgumentException> { SCPStatementPledgesXdr.fromXdrJsonElement(JsonNull) }
  }


  @Test
  fun sCPStatementXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPStatementXdr(sampleSCPStatementXdr(815, 0))
    assertRoundTripSCPStatementXdr(sampleSCPStatementXdr(815, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPStatementXdr(value: SCPStatementXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPStatementXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPStatementXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPStatementXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPStatementXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPStatementXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPStatementXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPStatementXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPStatementXdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCPEnvelopeXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPEnvelopeXdr(sampleSCPEnvelopeXdr(916, 0))
    assertRoundTripSCPEnvelopeXdr(sampleSCPEnvelopeXdr(916, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPEnvelopeXdr(value: SCPEnvelopeXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPEnvelopeXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPEnvelopeXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPEnvelopeXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPEnvelopeXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPEnvelopeXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPEnvelopeXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPEnvelopeXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPEnvelopeXdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCPQuorumSetXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCPQuorumSetXdr(sampleSCPQuorumSetXdr(1017, 0))
    assertRoundTripSCPQuorumSetXdr(sampleSCPQuorumSetXdr(1017, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCPQuorumSetXdr(value: SCPQuorumSetXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCPQuorumSetXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCPQuorumSetXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCPQuorumSetXdr.fromXdrJson(text).toXdrJson())

    val writer = XdrWriter()
    SCPQuorumSetXdr.fromXdrJsonElement(tree).encode(writer)
    assertEquals(tree, SCPQuorumSetXdr.decode(XdrReader(writer.toByteArray())).toXdrJsonElement())
  }

  @Test
  fun sCPQuorumSetXdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCPQuorumSetXdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCPQuorumSetXdr.fromXdrJsonElement(buildJsonObject { }) }
  }

}
