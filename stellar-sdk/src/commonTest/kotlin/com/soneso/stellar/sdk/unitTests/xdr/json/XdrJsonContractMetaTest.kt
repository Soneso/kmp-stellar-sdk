// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package com.soneso.stellar.sdk.unitTests.xdr.json

import com.soneso.stellar.sdk.xdr.*
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertFailsWith
import kotlinx.serialization.json.JsonNull
import kotlinx.serialization.json.JsonPrimitive
import kotlinx.serialization.json.buildJsonObject

internal fun sampleSCMetaV0Xdr(seed: Int, depth: Int): SCMetaV0Xdr = SCMetaV0Xdr(
  sampleString(seed, 64),
  sampleString(seed + 3, 64)
)

internal fun sampleSCMetaKindXdr(seed: Int): SCMetaKindXdr = SCMetaKindXdr.entries[seed.mod(SCMetaKindXdr.entries.size)]

internal fun sampleSCMetaEntryXdrV0Arm(seed: Int, depth: Int): SCMetaEntryXdr =
  SCMetaEntryXdr.V0(sampleSCMetaV0Xdr(seed, depth + 1))

internal fun sampleSCMetaEntryXdr(seed: Int, depth: Int): SCMetaEntryXdr =
  sampleSCMetaEntryXdrV0Arm(seed, depth)

/**
 * XDR-JSON (SEP-0051) rendering, decoding and rejection behaviour of the types
 * generated from Stellar-contract-meta.x.
 */
class XdrJsonContractMetaTest {

  @Test
  fun sCMetaV0XdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCMetaV0Xdr(sampleSCMetaV0Xdr(7, 0))
    assertRoundTripSCMetaV0Xdr(sampleSCMetaV0Xdr(7, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCMetaV0Xdr(value: SCMetaV0Xdr) {
    val tree = value.toXdrJsonElement()
    val text = SCMetaV0Xdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCMetaV0Xdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCMetaV0Xdr.fromXdrJson(text).toXdrJson())
  }

  @Test
  fun sCMetaV0XdrRejectsInputThatIsNotItsObject() {
    assertFailsWith<IllegalArgumentException> { SCMetaV0Xdr.fromXdrJsonElement(JsonNull) }
    assertFailsWith<IllegalArgumentException> { SCMetaV0Xdr.fromXdrJsonElement(buildJsonObject { }) }
  }


  @Test
  fun sCMetaKindXdrRendersScMetaV0AsScMetaV0() {
    val member = SCMetaKindXdr.SC_META_V0
    assertEquals(0, member.value)
    assertEquals(JsonPrimitive("sc_meta_v0"), member.toXdrJsonElement())
    assertEquals("\"sc_meta_v0\"", member.toXdrJson())
    assertEquals(member, SCMetaKindXdr.fromXdrJson("\"sc_meta_v0\""))
    assertEquals(member, SCMetaKindXdr.fromXdrJsonElement(JsonPrimitive("sc_meta_v0")))
  }

  @Test
  fun sCMetaKindXdrRejectsAMemberItDoesNotDeclare() {
    assertFailsWith<IllegalArgumentException> { SCMetaKindXdr.fromXdrJsonElement(JsonPrimitive("not_a_name_this_type_declares")) }
    assertFailsWith<IllegalArgumentException> { SCMetaKindXdr.fromXdrJsonElement(JsonNull) }
  }


  @Test
  fun sCMetaEntryXdrRoundTripsThroughItsJsonTree() {
    assertRoundTripSCMetaEntryXdr(sampleSCMetaEntryXdr(209, 0))
    assertRoundTripSCMetaEntryXdr(sampleSCMetaEntryXdr(209, SAMPLE_DEPTH))
  }

  private fun assertRoundTripSCMetaEntryXdr(value: SCMetaEntryXdr) {
    val tree = value.toXdrJsonElement()
    val text = SCMetaEntryXdr.fromXdrJsonElement(tree).toXdrJson()
    assertEquals(tree, SCMetaEntryXdr.fromXdrJson(text).toXdrJsonElement())
    assertEquals(text, SCMetaEntryXdr.fromXdrJson(text).toXdrJson())
  }

  @Test
  fun sCMetaEntryXdrRendersV0ArmAsScMetaV0() {
    val value = sampleSCMetaEntryXdrV0Arm(209, 0)
    val tree = value.toXdrJsonElement()
    assertEquals(listOf("sc_meta_v0"), jsonKeys(tree))
    val decoded = SCMetaEntryXdr.fromXdrJsonElement(tree)
    assertEquals(value.discriminant, decoded.discriminant)
    assertEquals(tree, decoded.toXdrJsonElement())
  }

  @Test
  fun sCMetaEntryXdrRejectsAnArmItDoesNotDeclare() {
    assertFailsWith<IllegalArgumentException> {
      SCMetaEntryXdr.fromXdrJsonElement(buildJsonObject { put("not_a_name_this_type_declares", JsonNull) })
    }
    assertFailsWith<IllegalArgumentException> {
      SCMetaEntryXdr.fromXdrJsonElement(
        buildJsonObject {
          put("not_a_name_this_type_declares", JsonNull)
          put("not_a_name_this_type_declares_too", JsonNull)
        }
      )
    }
    assertFailsWith<IllegalArgumentException> { SCMetaEntryXdr.fromXdrJsonElement(JsonNull) }
  }

}
