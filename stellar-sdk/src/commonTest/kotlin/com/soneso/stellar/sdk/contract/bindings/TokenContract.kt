//
// This file was generated by stellar_contract_bindings v0.5.0b0
// and stellar_sdk v13.0.0.
//
// This code requires kmp-stellar-sdk (Soneso) v1.9.0 or later.
//
// The BigInteger-typed 128/256-bit integer parameters and return values use
// com.ionspin.kotlin:bignum, which the SDK re-exports through its public API.
//
// @generated
//

package com.soneso.stellar.sdk.contract.bindings

import com.soneso.stellar.sdk.KeyPair
import com.soneso.stellar.sdk.Network
import com.soneso.stellar.sdk.Address
import com.soneso.stellar.sdk.contract.AssembledTransaction
import com.soneso.stellar.sdk.contract.ContractClient
import com.soneso.stellar.sdk.scval.Scv
import com.soneso.stellar.sdk.xdr.SCValXdr
import com.ionspin.kotlin.bignum.integer.BigInteger


/**
 * Generated struct TokenContractAllowanceDataKey
 */
data class TokenContractAllowanceDataKey(
    val from: Address,
    val spender: Address
) {
    fun toSCVal(): SCValXdr {
        val map = LinkedHashMap<SCValXdr, SCValXdr>()
        map[Scv.toSymbol("from")] = from.toSCVal()
        map[Scv.toSymbol("spender")] = spender.toSCVal()
        return Scv.toMap(map)
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): TokenContractAllowanceDataKey {
            val map = Scv.fromMap(scVal)
            return TokenContractAllowanceDataKey(
                from = Address.fromSCVal(map[Scv.toSymbol("from")]!!),
                spender = Address.fromSCVal(map[Scv.toSymbol("spender")]!!)
            )
        }
    }
}

/**
 * Generated struct TokenContractAllowanceValue
 */
data class TokenContractAllowanceValue(
    val amount: BigInteger,
    val expiration_ledger: UInt
) {
    fun toSCVal(): SCValXdr {
        val map = LinkedHashMap<SCValXdr, SCValXdr>()
        map[Scv.toSymbol("amount")] = Scv.toInt128(amount)
        map[Scv.toSymbol("expiration_ledger")] = Scv.toUint32(expiration_ledger)
        return Scv.toMap(map)
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): TokenContractAllowanceValue {
            val map = Scv.fromMap(scVal)
            return TokenContractAllowanceValue(
                amount = Scv.fromInt128(map[Scv.toSymbol("amount")]!!),
                expiration_ledger = Scv.fromUint32(map[Scv.toSymbol("expiration_ledger")]!!)
            )
        }
    }
}

/**
 * Generated union TokenContractDataKey
 */
sealed class TokenContractDataKey {
    abstract fun toSCVal(): SCValXdr

    data class Allowance(
        val value0: TokenContractAllowanceDataKey
    ) : TokenContractDataKey() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("Allowance"),
                value0.toSCVal()
            )
        )
    }

    data class Balance(
        val value0: Address
    ) : TokenContractDataKey() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("Balance"),
                value0.toSCVal()
            )
        )
    }

    data class State(
        val value0: Address
    ) : TokenContractDataKey() {
        override fun toSCVal(): SCValXdr = Scv.toVec(
            listOf(
                Scv.toSymbol("State"),
                value0.toSCVal()
            )
        )
    }

    object Admin : TokenContractDataKey() {
        override fun toSCVal(): SCValXdr = Scv.toVec(listOf(Scv.toSymbol("Admin")))
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): TokenContractDataKey {
            val elements = Scv.fromVec(scVal)
            return when (val tag = Scv.fromSymbol(elements[0])) {
                "Allowance" -> Allowance(
                    TokenContractAllowanceDataKey.fromSCVal(elements[1])
                )
                "Balance" -> Balance(
                    Address.fromSCVal(elements[1])
                )
                "State" -> State(
                    Address.fromSCVal(elements[1])
                )
                "Admin" -> Admin
                else -> throw IllegalArgumentException("Unknown TokenContractDataKey tag: $tag")
            }
        }
    }
}

/**
 * Generated struct TokenContractTokenMetadata
 */
data class TokenContractTokenMetadata(
    val decimal: UInt,
    val name: String,
    val symbol: String
) {
    fun toSCVal(): SCValXdr {
        val map = LinkedHashMap<SCValXdr, SCValXdr>()
        map[Scv.toSymbol("decimal")] = Scv.toUint32(decimal)
        map[Scv.toSymbol("name")] = Scv.toString(name)
        map[Scv.toSymbol("symbol")] = Scv.toString(symbol)
        return Scv.toMap(map)
    }

    companion object {
        fun fromSCVal(scVal: SCValXdr): TokenContractTokenMetadata {
            val map = Scv.fromMap(scVal)
            return TokenContractTokenMetadata(
                decimal = Scv.fromUint32(map[Scv.toSymbol("decimal")]!!),
                name = Scv.fromString(map[Scv.toSymbol("name")]!!),
                symbol = Scv.fromString(map[Scv.toSymbol("symbol")]!!)
            )
        }
    }
}

/**
 * Generated contract client for TokenContract.
 */
class TokenContract internal constructor(val client: ContractClient) {

    companion object {
        /**
         * Create a client for a deployed contract without loading its spec from the network.
         *
         * @param contractId The contract ID (C... address)
         * @param rpcUrl The Soroban RPC server URL
         * @param network The network the contract is deployed on
         */
        fun forContract(contractId: String, rpcUrl: String, network: Network): TokenContract =
            TokenContract(ContractClient.forContractWithoutSpec(contractId, rpcUrl, network))
    }

    /**
     * Invoke the mint contract function.
     * @param to Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun mint(
        to: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "mint",
            parameters = listOf(to.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the mint contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param to Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildMintTx(
        to: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "mint",
            parameters = listOf(to.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the set_admin contract function.
     * @param newAdmin Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun setAdmin(
        newAdmin: Address,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "set_admin",
            parameters = listOf(newAdmin.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the set_admin contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param newAdmin Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildSetAdminTx(
        newAdmin: Address,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "set_admin",
            parameters = listOf(newAdmin.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the allowance contract function.
     * @param from Address
     * @param spender Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BigInteger
     */
    suspend fun allowance(
        from: Address,
        spender: Address,
        source: String,
        signer: KeyPair?
    ): BigInteger =
        client.invoke(
            functionName = "allowance",
            parameters = listOf(from.toSCVal(), spender.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt128(result) }
        )

    /**
     * Build an [AssembledTransaction] for the allowance contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param from Address
     * @param spender Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildAllowanceTx(
        from: Address,
        spender: Address,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BigInteger> =
        client.buildInvoke(
            functionName = "allowance",
            parameters = listOf(from.toSCVal(), spender.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt128(result) }
        )

    /**
     * Invoke the approve contract function.
     * @param from Address
     * @param spender Address
     * @param amount BigInteger
     * @param expirationLedger UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun approve(
        from: Address,
        spender: Address,
        amount: BigInteger,
        expirationLedger: UInt,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "approve",
            parameters = listOf(from.toSCVal(), spender.toSCVal(), Scv.toInt128(amount), Scv.toUint32(expirationLedger)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the approve contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param from Address
     * @param spender Address
     * @param amount BigInteger
     * @param expirationLedger UInt
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildApproveTx(
        from: Address,
        spender: Address,
        amount: BigInteger,
        expirationLedger: UInt,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "approve",
            parameters = listOf(from.toSCVal(), spender.toSCVal(), Scv.toInt128(amount), Scv.toUint32(expirationLedger)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the balance contract function.
     * @param id Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return BigInteger
     */
    suspend fun balance(
        id: Address,
        source: String,
        signer: KeyPair?
    ): BigInteger =
        client.invoke(
            functionName = "balance",
            parameters = listOf(id.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt128(result) }
        )

    /**
     * Build an [AssembledTransaction] for the balance contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param id Address
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildBalanceTx(
        id: Address,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<BigInteger> =
        client.buildInvoke(
            functionName = "balance",
            parameters = listOf(id.toSCVal()),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromInt128(result) }
        )

    /**
     * Invoke the transfer contract function.
     * @param from Address
     * @param toMuxed Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun transfer(
        from: Address,
        toMuxed: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "transfer",
            parameters = listOf(from.toSCVal(), toMuxed.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the transfer contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param from Address
     * @param toMuxed Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildTransferTx(
        from: Address,
        toMuxed: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "transfer",
            parameters = listOf(from.toSCVal(), toMuxed.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the transfer_from contract function.
     * @param spender Address
     * @param from Address
     * @param to Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun transferFrom(
        spender: Address,
        from: Address,
        to: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "transfer_from",
            parameters = listOf(spender.toSCVal(), from.toSCVal(), to.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the transfer_from contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param spender Address
     * @param from Address
     * @param to Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildTransferFromTx(
        spender: Address,
        from: Address,
        to: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "transfer_from",
            parameters = listOf(spender.toSCVal(), from.toSCVal(), to.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the burn contract function.
     * @param from Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun burn(
        from: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "burn",
            parameters = listOf(from.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the burn contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param from Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildBurnTx(
        from: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "burn",
            parameters = listOf(from.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the burn_from contract function.
     * @param spender Address
     * @param from Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun burnFrom(
        spender: Address,
        from: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ) {
        client.invoke(
            functionName = "burn_from",
            parameters = listOf(spender.toSCVal(), from.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )
    }

    /**
     * Build an [AssembledTransaction] for the burn_from contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param spender Address
     * @param from Address
     * @param amount BigInteger
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildBurnFromTx(
        spender: Address,
        from: Address,
        amount: BigInteger,
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<Unit> =
        client.buildInvoke(
            functionName = "burn_from",
            parameters = listOf(spender.toSCVal(), from.toSCVal(), Scv.toInt128(amount)),
            source = source,
            signer = signer,
            parseResultXdrFn = { }
        )

    /**
     * Invoke the decimals contract function.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return UInt
     */
    suspend fun decimals(
        source: String,
        signer: KeyPair?
    ): UInt =
        client.invoke(
            functionName = "decimals",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Build an [AssembledTransaction] for the decimals contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildDecimalsTx(
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<UInt> =
        client.buildInvoke(
            functionName = "decimals",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromUint32(result) }
        )

    /**
     * Invoke the name contract function.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return String
     */
    suspend fun name(
        source: String,
        signer: KeyPair?
    ): String =
        client.invoke(
            functionName = "name",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromString(result) }
        )

    /**
     * Build an [AssembledTransaction] for the name contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildNameTx(
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<String> =
        client.buildInvoke(
            functionName = "name",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromString(result) }
        )

    /**
     * Invoke the symbol contract function.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     * @return String
     */
    suspend fun symbol(
        source: String,
        signer: KeyPair?
    ): String =
        client.invoke(
            functionName = "symbol",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromString(result) }
        )

    /**
     * Build an [AssembledTransaction] for the symbol contract function.
     *
     * Use this when you need to inspect or manipulate the transaction (memos, additional
     * signatures, preconditions) before signing and submitting.
     * @param source The source account (G... or M... address)
     * @param signer KeyPair for signing; null for read-only calls
     */
    suspend fun buildSymbolTx(
        source: String,
        signer: KeyPair?
    ): AssembledTransaction<String> =
        client.buildInvoke(
            functionName = "symbol",
            parameters = listOf(),
            source = source,
            signer = signer,
            parseResultXdrFn = { result -> Scv.fromString(result) }
        )
}
