// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package com.soneso.stellar.sdk.xdr

import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject
import kotlinx.serialization.json.buildJsonObject

private const val XDR_JSON_TYPE = "TrustLineEntryV1ExtXdr"

/**
 * XDR Source:
 * union switch (int v)
 *             {
 *             case 0:
 *                 void;
 *             case 2:
 *                 TrustLineEntryExtensionV2 v2;
 *             }
 */
sealed class TrustLineEntryV1ExtXdr {
  abstract val discriminant: Int

  data class V2(
    val value: TrustLineEntryExtensionV2Xdr
  ) : TrustLineEntryV1ExtXdr() {
    override val discriminant: Int = 2
  }

  data object Void : TrustLineEntryV1ExtXdr() {
    override val discriminant: Int = 0
  }

  companion object {

    fun decode(reader: XdrReader): TrustLineEntryV1ExtXdr {
      val discriminant = reader.readInt()
      return when (discriminant) {
        0 -> Void
        2 -> {
          val value = TrustLineEntryExtensionV2Xdr.decode(reader)
          V2(value)
        }
        else -> throw IllegalArgumentException("Unknown TrustLineEntryV1ExtXdr discriminant: $discriminant")
      }
    }

    fun fromXdrJson(json: String): TrustLineEntryV1ExtXdr = fromXdrJsonTree(XdrJson.parse(json, XDR_JSON_TYPE))

    fun fromXdrJsonElement(element: JsonElement): TrustLineEntryV1ExtXdr = fromXdrJsonTree(XdrJson.checkDepth(element, XDR_JSON_TYPE))

    internal fun fromXdrJsonTree(element: JsonElement): TrustLineEntryV1ExtXdr {
      if (element is JsonObject) {
        val (arm, value) = XdrJson.singleKeyObject(element, XDR_JSON_TYPE)
        return when (arm) {
          "v2" -> V2(TrustLineEntryExtensionV2Xdr.fromXdrJsonTree(value))
          else -> XdrJson.unknownArm(XDR_JSON_TYPE, arm)
        }
      }
      return when (val arm = XdrJson.name(element, XDR_JSON_TYPE)) {
        "v0" -> Void
        else -> XdrJson.unknownArm(XDR_JSON_TYPE, arm)
      }
    }
  }

  fun encode(writer: XdrWriter) {
    writer.writeInt(discriminant)
    when (this) {
      is Void -> {}
      is V2 -> {
        value.encode(writer)
      }
    }
  }

  fun toXdrJsonElement(): JsonElement = when (this) {
    is Void -> XdrJson.name("v0")
    is V2 -> buildJsonObject { put("v2", value.toXdrJsonElement()) }
  }

  fun toXdrJson(): String = XdrJson.encodeToString(toXdrJsonElement())
}
