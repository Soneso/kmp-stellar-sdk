// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package com.soneso.stellar.sdk.xdr

/**
 * XDR Source:
 * struct SCPQuorumSet
 * {
 *     uint32 threshold;
 *     NodeID validators<>;
 *     SCPQuorumSet innerSets<>;
 * };
 */
data class SCPQuorumSetXdr(
  val threshold: Uint32Xdr,
  val validators: List<NodeIDXdr>,
  val innerSets: List<SCPQuorumSetXdr>
) {
  companion object {

    fun decode(reader: XdrReader): SCPQuorumSetXdr {
      val threshold = Uint32Xdr.decode(reader)
      val validators = List(reader.readInt()) { NodeIDXdr.decode(reader) }
      val innerSets = List(reader.readInt()) { SCPQuorumSetXdr.decode(reader) }
      return SCPQuorumSetXdr(threshold, validators, innerSets)
    }
  }

  fun encode(writer: XdrWriter) {
    threshold.encode(writer)
    writer.writeInt(validators.size)
    validators.forEach { item ->
      item.encode(writer)
    }
    writer.writeInt(innerSets.size)
    innerSets.forEach { item ->
      item.encode(writer)
    }
  }
}
