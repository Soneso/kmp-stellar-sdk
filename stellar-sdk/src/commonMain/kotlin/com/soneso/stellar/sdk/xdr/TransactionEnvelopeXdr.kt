// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package com.soneso.stellar.sdk.xdr

import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.buildJsonObject

private const val XDR_JSON_TYPE = "TransactionEnvelopeXdr"

/**
 * XDR Source:
 * union TransactionEnvelope switch (EnvelopeType type)
 * {
 * case ENVELOPE_TYPE_TX_V0:
 *     TransactionV0Envelope v0;
 * case ENVELOPE_TYPE_TX:
 *     TransactionV1Envelope v1;
 * case ENVELOPE_TYPE_TX_FEE_BUMP:
 *     FeeBumpTransactionEnvelope feeBump;
 * };
 */
sealed class TransactionEnvelopeXdr {
  abstract val discriminant: EnvelopeTypeXdr

  data class V0(
    val value: TransactionV0EnvelopeXdr
  ) : TransactionEnvelopeXdr() {
    override val discriminant: EnvelopeTypeXdr = EnvelopeTypeXdr.ENVELOPE_TYPE_TX_V0
  }

  data class V1(
    val value: TransactionV1EnvelopeXdr
  ) : TransactionEnvelopeXdr() {
    override val discriminant: EnvelopeTypeXdr = EnvelopeTypeXdr.ENVELOPE_TYPE_TX
  }

  data class FeeBump(
    val value: FeeBumpTransactionEnvelopeXdr
  ) : TransactionEnvelopeXdr() {
    override val discriminant: EnvelopeTypeXdr = EnvelopeTypeXdr.ENVELOPE_TYPE_TX_FEE_BUMP
  }

  companion object {

    fun decode(reader: XdrReader): TransactionEnvelopeXdr {
      val discriminant = EnvelopeTypeXdr.decode(reader)
      return when (discriminant) {
        EnvelopeTypeXdr.ENVELOPE_TYPE_TX_V0 -> {
          val value = TransactionV0EnvelopeXdr.decode(reader)
          V0(value)
        }
        EnvelopeTypeXdr.ENVELOPE_TYPE_TX -> {
          val value = TransactionV1EnvelopeXdr.decode(reader)
          V1(value)
        }
        EnvelopeTypeXdr.ENVELOPE_TYPE_TX_FEE_BUMP -> {
          val value = FeeBumpTransactionEnvelopeXdr.decode(reader)
          FeeBump(value)
        }
        else -> throw IllegalArgumentException("Unknown TransactionEnvelopeXdr discriminant: $discriminant")
      }
    }

    fun fromXdrJson(json: String): TransactionEnvelopeXdr = fromXdrJsonTree(XdrJson.parse(json, XDR_JSON_TYPE))

    fun fromXdrJsonElement(element: JsonElement): TransactionEnvelopeXdr = fromXdrJsonTree(XdrJson.checkDepth(element, XDR_JSON_TYPE))

    internal fun fromXdrJsonTree(element: JsonElement): TransactionEnvelopeXdr {
      val (arm, value) = XdrJson.singleKeyObject(element, XDR_JSON_TYPE)
      return when (arm) {
        "tx_v0" -> V0(TransactionV0EnvelopeXdr.fromXdrJsonTree(value))
        "tx" -> V1(TransactionV1EnvelopeXdr.fromXdrJsonTree(value))
        "tx_fee_bump" -> FeeBump(FeeBumpTransactionEnvelopeXdr.fromXdrJsonTree(value))
        else -> XdrJson.unknownArm(XDR_JSON_TYPE, arm)
      }
    }
  }

  fun encode(writer: XdrWriter) {
    discriminant.encode(writer)
    when (this) {
      is V0 -> {
        value.encode(writer)
      }
      is V1 -> {
        value.encode(writer)
      }
      is FeeBump -> {
        value.encode(writer)
      }
    }
  }

  fun toXdrJsonElement(): JsonElement = when (this) {
    is V0 -> buildJsonObject { put("tx_v0", value.toXdrJsonElement()) }
    is V1 -> buildJsonObject { put("tx", value.toXdrJsonElement()) }
    is FeeBump -> buildJsonObject { put("tx_fee_bump", value.toXdrJsonElement()) }
  }

  fun toXdrJson(): String = XdrJson.encodeToString(toXdrJsonElement())
}
