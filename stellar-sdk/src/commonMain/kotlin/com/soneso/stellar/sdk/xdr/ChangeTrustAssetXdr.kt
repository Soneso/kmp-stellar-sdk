// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package com.soneso.stellar.sdk.xdr

import kotlinx.serialization.json.JsonElement
import kotlinx.serialization.json.JsonObject
import kotlinx.serialization.json.buildJsonObject

private const val XDR_JSON_TYPE = "ChangeTrustAssetXdr"

/**
 * XDR Source:
 * union ChangeTrustAsset switch (AssetType type)
 * {
 * case ASSET_TYPE_NATIVE: // Not credit
 *     void;
 * 
 * case ASSET_TYPE_CREDIT_ALPHANUM4:
 *     AlphaNum4 alphaNum4;
 * 
 * case ASSET_TYPE_CREDIT_ALPHANUM12:
 *     AlphaNum12 alphaNum12;
 * 
 * case ASSET_TYPE_POOL_SHARE:
 *     LiquidityPoolParameters liquidityPool;
 * 
 *     // add other asset types here in the future
 * };
 */
sealed class ChangeTrustAssetXdr {
  abstract val discriminant: AssetTypeXdr

  data class AlphaNum4(
    val value: AlphaNum4Xdr
  ) : ChangeTrustAssetXdr() {
    override val discriminant: AssetTypeXdr = AssetTypeXdr.ASSET_TYPE_CREDIT_ALPHANUM4
  }

  data class AlphaNum12(
    val value: AlphaNum12Xdr
  ) : ChangeTrustAssetXdr() {
    override val discriminant: AssetTypeXdr = AssetTypeXdr.ASSET_TYPE_CREDIT_ALPHANUM12
  }

  data class LiquidityPool(
    val value: LiquidityPoolParametersXdr
  ) : ChangeTrustAssetXdr() {
    override val discriminant: AssetTypeXdr = AssetTypeXdr.ASSET_TYPE_POOL_SHARE
  }

  /** Not credit */
  data object Void : ChangeTrustAssetXdr() {
    override val discriminant: AssetTypeXdr = AssetTypeXdr.ASSET_TYPE_NATIVE
  }

  companion object {

    fun decode(reader: XdrReader): ChangeTrustAssetXdr {
      val discriminant = AssetTypeXdr.decode(reader)
      return when (discriminant) {
        AssetTypeXdr.ASSET_TYPE_NATIVE -> Void
        AssetTypeXdr.ASSET_TYPE_CREDIT_ALPHANUM4 -> {
          val value = AlphaNum4Xdr.decode(reader)
          AlphaNum4(value)
        }
        AssetTypeXdr.ASSET_TYPE_CREDIT_ALPHANUM12 -> {
          val value = AlphaNum12Xdr.decode(reader)
          AlphaNum12(value)
        }
        AssetTypeXdr.ASSET_TYPE_POOL_SHARE -> {
          val value = LiquidityPoolParametersXdr.decode(reader)
          LiquidityPool(value)
        }
        else -> throw IllegalArgumentException("Unknown ChangeTrustAssetXdr discriminant: $discriminant")
      }
    }

    fun fromXdrJson(json: String): ChangeTrustAssetXdr = fromXdrJsonTree(XdrJson.parse(json, XDR_JSON_TYPE))

    fun fromXdrJsonElement(element: JsonElement): ChangeTrustAssetXdr = fromXdrJsonTree(XdrJson.checkDepth(element, XDR_JSON_TYPE))

    internal fun fromXdrJsonTree(element: JsonElement): ChangeTrustAssetXdr {
      if (element is JsonObject) {
        val (arm, value) = XdrJson.singleKeyObject(element, XDR_JSON_TYPE)
        return when (arm) {
          "credit_alphanum4" -> AlphaNum4(AlphaNum4Xdr.fromXdrJsonTree(value))
          "credit_alphanum12" -> AlphaNum12(AlphaNum12Xdr.fromXdrJsonTree(value))
          "pool_share" -> LiquidityPool(LiquidityPoolParametersXdr.fromXdrJsonTree(value))
          else -> XdrJson.unknownArm(XDR_JSON_TYPE, arm)
        }
      }
      return when (val arm = XdrJson.name(element, XDR_JSON_TYPE)) {
        "native" -> Void
        else -> XdrJson.unknownArm(XDR_JSON_TYPE, arm)
      }
    }
  }

  fun encode(writer: XdrWriter) {
    discriminant.encode(writer)
    when (this) {
      is Void -> {}
      is AlphaNum4 -> {
        value.encode(writer)
      }
      is AlphaNum12 -> {
        value.encode(writer)
      }
      is LiquidityPool -> {
        value.encode(writer)
      }
    }
  }

  fun toXdrJsonElement(): JsonElement = when (this) {
    is Void -> XdrJson.name("native")
    is AlphaNum4 -> buildJsonObject { put("credit_alphanum4", value.toXdrJsonElement()) }
    is AlphaNum12 -> buildJsonObject { put("credit_alphanum12", value.toXdrJsonElement()) }
    is LiquidityPool -> buildJsonObject { put("pool_share", value.toXdrJsonElement()) }
  }

  fun toXdrJson(): String = XdrJson.encodeToString(toXdrJsonElement())
}
