// Automatically generated by xdrgen
// DO NOT EDIT or your changes may be overwritten

package com.stellar.sdk.xdr

/**
 * XDR Source:
 * struct ContractCostParamEntry {
 *     // use `ext` to add more terms (e.g. higher order polynomials) in the future
 *     ExtensionPoint ext;
 * 
 *     int64 constTerm;
 *     int64 linearTerm;
 * };
 */
data class ContractCostParamEntryXdr(
  /** use `ext` to add more terms (e.g. higher order polynomials) in the future */
  val ext: ExtensionPointXdr,
  val constTerm: Int64Xdr,
  val linearTerm: Int64Xdr
) {
  companion object {

    fun decode(reader: XdrReader): ContractCostParamEntryXdr {
      val ext = ExtensionPointXdr.decode(reader)
      val constTerm = Int64Xdr.decode(reader)
      val linearTerm = Int64Xdr.decode(reader)
      return ContractCostParamEntryXdr(ext, constTerm, linearTerm)
    }
  }

  fun encode(writer: XdrWriter) {
    ext.encode(writer)
    constTerm.encode(writer)
    linearTerm.encode(writer)
  }
}
