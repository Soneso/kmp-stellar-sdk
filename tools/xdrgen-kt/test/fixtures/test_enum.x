// Test fixture: Enum types
// Covers simple enums with multiple members and comments.

enum Color
{
    // Red color
    RED = 0,
    // Green color
    GREEN = 1,
    BLUE = 2
};

enum StatusCode
{
    STATUS_OK = 0,
    STATUS_NOT_FOUND = 1,
    STATUS_ERROR = 2,
    STATUS_UNKNOWN = -1
};
